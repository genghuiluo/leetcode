package cn.genghuiluo.sort;

/*
* 堆排序
* 1. 将待排序的序列构造成一个大顶堆。此时，整个序列的最大值就是堆顶的根节点。
* 2. 将它移走(其实就是将其与堆数组的末尾元素交换，此时末尾元素就是最大值)，然后将剩余的n-1个序列重新构造成一个堆，这样就会得到n个元素中的次最大值。
* 3. 如此反复执行，就能得到一个有序序列了。
*
* https://blog.csdn.net/zdp072/article/details/44227317
* */
public class HeapSort {
}
